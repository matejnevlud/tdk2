[project]
name = "tdk2-traceability"
version = "0.1.0"
description = "PLC Traceability & Camera Image Archiving Application"
requires-python = ">=3.10"
dependencies = [
    "python-snap7>=2.0.2",
]

[project.optional-dependencies]
dev = [
    "pyinstaller>=6.0",
]